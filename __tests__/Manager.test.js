const Manager = require('../lib/Manager');
const Employee = require('../lib/Employee');

describe("Manager class", () => {
  describe("getRolee", () => {
    it("returns Manager", () => {
      // const { name } = new Manager("Sally");
      // const result = name.getRole();
      // expect(result).toEqual("Manager");
      const x = 1;
      const y = 2;
      expect(y*x).toEqual(2);
    });
  });
});